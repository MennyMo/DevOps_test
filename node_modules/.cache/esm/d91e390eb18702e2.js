_b18‍.x([["default",()=>_b18‍.o]]);const mongoose = require('mongoose');

const { Schema, connection } = mongoose;

const userSchema = new Schema({
    id: {
        type: Number,
        required: true
    },
    name: {
        type: String,
        required: true
    }
});

const UserModel = connection.model('user', userSchema);

_b18‍.d(UserModel);

let json,urlencoded,cors,helmet,config;_338‍.x([["default",()=>_338‍.o]]);_338‍.w("express",[["json",["json"],function(v){json=v}],["urlencoded",["urlencoded"],function(v){urlencoded=v}]]);_338‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_338‍.w("helmet",[["default",["helmet"],function(v){helmet=v}]]);_338‍.w("./env",[["default",["config"],function(v){config=v}]]);





const Ddos =  require('ddos');

const app = exp => {
    exp.use(helmet());
    exp.use(cors());

    const ddos = new Ddos();
    exp.use(ddos.express);
    exp.use(json({ limit: '2mb' }));
    exp.use(urlencoded({ limit: '2mb', extended: true }));

    exp.get('/', (req, res) => {
        return res.status(200).json({
            message: 'Welcome to Molly app.'
        });
    });

    // register route here

    exp.use((req, res) => {
        return res.status(404).json({
            message: 'Route does not exist.'
        });
    });

    const port = config.PORT || 3000;

    const server = exp.listen(port, () => {
        console.log(`App is running on ${port}`);
    });

    server.setTimeout(20000);

};

_338‍.d(app);

